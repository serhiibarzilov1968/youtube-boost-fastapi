# YouTubeBoost

Инструмент для анализа и оптимизации YouTube каналов.

## Возможности

- 📊 **Анализ канала** - Получите детальную статистику вашего YouTube канала в реальном времени
- 💡 **Идеи контента** - Получайте рекомендации по созданию контента на основе анализа трендов
- 🎨 **Дизайн миниатюр** - Автоматически создавайте привлекательные миниатюры для ваших видео
- 📈 **SEO оптимизация** - Улучшайте видимость ваших видео с помощью SEO рекомендаций

## Структура проекта

```
youtube_boost_clean/
├── main.py                 # Главный файл FastAPI приложения
├── database.py             # Конфигурация базы данных
├── models.py               # SQLAlchemy модели
├── schemas.py              # Pydantic схемы
├── auth.py                 # Логика аутентификации
├── requirements.txt        # Зависимости Python
├── .env.example            # Пример файла переменных окружения
├── .gitignore              # Git ignore файл
├── README.md               # Этот файл
├── frontend/               # Фронтенд приложение
│   ├── index.html          # Главная страница
│   ├── login.html          # Страница входа
│   ├── dashboard.html      # Панель управления
│   └── app.js              # JavaScript логика
└── services/               # Сервисы приложения
    ├── __init__.py
    ├── youtube_analyzer.py # Анализатор YouTube каналов
    ├── image_generator.py  # Генератор изображений
    └── shorts_generator.py # Генератор шортсов
```

## Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/serhiibarzilov1968/youtube-boost-fastapi.git
cd youtube-boost-fastapi
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Создайте файл .env

Скопируйте `.env.example` в `.env` и добавьте ваш YouTube API ключ:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```
YOUTUBE_API_KEY=ваш_ключ_api_здесь
```

### 5. Запустите приложение

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8001
```

Приложение будет доступно на `http://localhost:8001`

### 6. Запустите фронтенд

В другом терминале:

```bash
cd frontend
python -m http.server 8000
```

Фронтенд будет доступен на `http://localhost:8000`

## Получение YouTube API ключа

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект
3. Включите **YouTube Data API v3**
4. Создайте **API Key** в разделе "Credentials"
5. Скопируйте ключ в файл `.env`

## API Endpoints

### Аутентификация

- `POST /api/v1/users/register` - Регистрация нового пользователя
- `POST /api/v1/users/login` - Вход в систему

### Анализатор YouTube

- `GET /api/v1/analyzer/analyze/{channel_id}` - Анализ YouTube канала

## Примеры использования

### Регистрация

```bash
curl -X POST "http://localhost:8001/api/v1/users/register" \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","password":"password123"}'
```

### Вход

```bash
curl -X POST "http://localhost:8001/api/v1/users/login" \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","password":"password123"}'
```

### Анализ канала

```bash
curl -X GET "http://localhost:8001/api/v1/analyzer/analyze/UC_x5XG1OV2P6uZZ5FSM9Ttw"
```

## Развертывание на Render

1. Залейте код на GitHub
2. Создайте новый Web Service на [Render](https://render.com/)
3. Подключите ваш GitHub репозиторий
4. Установите переменную окружения `YOUTUBE_API_KEY`
5. Установите команду запуска: `uvicorn main:app --host 0.0.0.0 --port $PORT`

## Лицензия

MIT

## Контакты

Для вопросов и предложений создавайте Issues на GitHub.

